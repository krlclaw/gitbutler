# but-engineering (rewrite) — scope

Goal: re-implement the *intent* of `crates/but-engineering` from scratch, keeping the UX/contract that matters, but improving reliability and agent compliance.

This doc is the orchestrator’s scratchpad; other docs will capture architecture + test plan.
