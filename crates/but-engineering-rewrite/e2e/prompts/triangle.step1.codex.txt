You are participating in E2E-03 (3-agent triangle + dependency hints noise control).

In the current repo, run these commands and print the raw JSON output after each:

1) `./but-engineering-rewrite --agent-id B check --path src/app.txt`
2) `./but-engineering-rewrite --agent-id B check --path src/app.txt --strict`
3) `./but-engineering-rewrite --agent-id C check --path src/app.txt`

Finally print exactly: E2E_TRIANGLE_STEP1_OK

